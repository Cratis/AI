// @generated by Cratis. Source: Planner.Issues.Grouping.SettingPrompt.SetGroupPrompt, Planner.Issues.Grouping.SettingPrompt.GroupPromptSet. Time: 2026-08-08T21:11:05.5434570Z. Hash: CDA5CBA82A3E85FECC1D44C51CCFCC7F604E27E79F4AA9DF494893072B53FD15
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/
/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { field } from '@cratis/fundamentals';

/**
 * Command for setting the extra instructions that travel with a group when an agent works on it.
 */
export interface ISetGroupPrompt {
    
    /**
     * The identity of the group.
     */
    group?: string;
    
    /**
     * The instructions - an empty prompt clears them.
     */
    prompt?: string;
}


/**
 * Command for setting the extra instructions that travel with a group when an agent works on it.
 */
export class SetGroupPrompt extends Command<ISetGroupPrompt, GroupPromptSet> implements ISetGroupPrompt {
    readonly route: string = '/api/issues/grouping/setting-prompt';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('group', String, false),
        new PropertyDescriptor('prompt', String, false),
    ];

    private _group!: string;
    private _prompt!: string;

    constructor() {
        super(GroupPromptSet, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The identity of the group.
     */
    get group(): string {
        return this._group;
    }

    set group(value: string) {
        this._group = value;
        this.propertyChanged('group');
    }
    /**
     * The instructions - an empty prompt clears them.
     */
    get prompt(): string {
        return this._prompt;
    }

    set prompt(value: string) {
        this._prompt = value;
        this.propertyChanged('prompt');
    }

    static use(initialValues?: ISetGroupPrompt): [SetGroupPrompt, SetCommandValues<ISetGroupPrompt>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<SetGroupPrompt, ISetGroupPrompt>(SetGroupPrompt, initialValues);
    }
}

/**
 * Event raised when the extra instructions for a group have been set - they are appended to the prompt the agent gets when working on the group.
 */
export class GroupPromptSet {

    /**
     * The instructions - the not-set sentinel when cleared.
     */
    @field(String)
    prompt!: string;
}
