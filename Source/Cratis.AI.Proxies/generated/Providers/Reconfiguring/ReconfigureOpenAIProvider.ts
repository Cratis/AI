// @generated by Cratis. Source: Cratis.AI.Providers.Reconfiguring.ReconfigureOpenAIProvider. Time: 2026-09-15T19:08:34.0280380Z. Hash: AFE978AE0A73F64BAF92AE796B24F2FD2C4B32D4ED7FB11D81B5AA79DF460170
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { OpenAIProviderReconfigured } from './OpenAIProviderReconfigured';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for changing an OpenAI provider's API key - a blank value keeps whatever is already recorded. Ported from Direct's `AIProviders.Reconfiguring.ReconfigureOpenAIProvider` (plan Section 5.2 step 4) - deliberately without Direct's own subscription-credential-kind classification event, the same simplification {@link AddOpenAIProvider} makes and for the same reason (plan Section 5.6, not yet ported).
 */
export interface IReconfigureOpenAIProvider {
    
    /**
     * The new API key - blank keeps the current one.
     */
    apiKey?: string;
    
    /**
     * The provider to reconfigure.
     */
    provider?: Guid;
}


/**
 * Command for changing an OpenAI provider's API key - a blank value keeps whatever is already recorded. Ported from Direct's `AIProviders.Reconfiguring.ReconfigureOpenAIProvider` (plan Section 5.2 step 4) - deliberately without Direct's own subscription-credential-kind classification event, the same simplification {@link AddOpenAIProvider} makes and for the same reason (plan Section 5.6, not yet ported).
 */
export class ReconfigureOpenAIProvider extends Command<IReconfigureOpenAIProvider, OpenAIProviderReconfigured> implements IReconfigureOpenAIProvider {
    readonly route: string = '/api/a-i/providers/reconfiguring/reconfigure-open-a-i-provider';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('apiKey', String, false),
        new PropertyDescriptor('provider', Guid, false),
    ];

    private _apiKey!: string;
    private _provider!: Guid;

    constructor() {
        super(OpenAIProviderReconfigured, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The new API key - blank keeps the current one.
     */
    get apiKey(): string {
        return this._apiKey;
    }

    set apiKey(value: string) {
        this._apiKey = value;
        this.propertyChanged('apiKey');
    }
    /**
     * The provider to reconfigure.
     */
    get provider(): Guid {
        return this._provider;
    }

    set provider(value: Guid) {
        this._provider = value;
        this.propertyChanged('provider');
    }

    static use(initialValues?: IReconfigureOpenAIProvider): [ReconfigureOpenAIProvider, SetCommandValues<IReconfigureOpenAIProvider>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<ReconfigureOpenAIProvider, IReconfigureOpenAIProvider>(ReconfigureOpenAIProvider, initialValues);
    }
}
