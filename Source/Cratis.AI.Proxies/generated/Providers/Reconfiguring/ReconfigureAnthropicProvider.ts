// @generated by Cratis. Source: Cratis.AI.Providers.Reconfiguring.ReconfigureAnthropicProvider. Time: 2026-09-16T09:03:39.3662250Z. Hash: BB1FB07EC7B9C353DA5FB7A930ABABA7C6703AA26F7E7B82093BE4798C59DF39
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { AnthropicProviderReconfigured } from './AnthropicProviderReconfigured';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for changing an Anthropic provider's API key - a blank value keeps whatever is already recorded, rather than clearing it. Ported from Direct's `AIProviders.Reconfiguring.ReconfigureAnthropicProvider` (plan Section 5.2 step 4), rewired onto the package's own {@link ISecretProtector}.
 */
export interface IReconfigureAnthropicProvider {
    
    /**
     * The new API key - blank keeps the current one.
     */
    apiKey?: string;
    
    /**
     * The provider to reconfigure.
     */
    provider?: Guid;
}


/**
 * Command for changing an Anthropic provider's API key - a blank value keeps whatever is already recorded, rather than clearing it. Ported from Direct's `AIProviders.Reconfiguring.ReconfigureAnthropicProvider` (plan Section 5.2 step 4), rewired onto the package's own {@link ISecretProtector}.
 */
export class ReconfigureAnthropicProvider extends Command<IReconfigureAnthropicProvider, AnthropicProviderReconfigured> implements IReconfigureAnthropicProvider {
    readonly route: string = '/api/a-i/providers/reconfiguring/reconfigure-anthropic-provider';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('apiKey', String, false),
        new PropertyDescriptor('provider', Guid, false),
    ];

    private _apiKey!: string;
    private _provider!: Guid;

    constructor() {
        super(AnthropicProviderReconfigured, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The new API key - blank keeps the current one.
     */
    get apiKey(): string {
        return this._apiKey;
    }

    set apiKey(value: string) {
        this._apiKey = value;
        this.propertyChanged('apiKey');
    }
    /**
     * The provider to reconfigure.
     */
    get provider(): Guid {
        return this._provider;
    }

    set provider(value: Guid) {
        this._provider = value;
        this.propertyChanged('provider');
    }

    static use(initialValues?: IReconfigureAnthropicProvider): [ReconfigureAnthropicProvider, SetCommandValues<IReconfigureAnthropicProvider>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<ReconfigureAnthropicProvider, IReconfigureAnthropicProvider>(ReconfigureAnthropicProvider, initialValues);
    }
}
