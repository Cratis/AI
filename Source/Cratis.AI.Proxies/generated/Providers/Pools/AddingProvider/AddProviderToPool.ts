// @generated by Cratis. Source: Cratis.AI.Providers.Pools.AddingProvider.AddProviderToPool. Time: 2026-09-24T20:35:16.9475720Z. Hash: 70ADB7B2708967047917EBCB71F2158148C9BD9E3C7D3643C3244F6158C29F6B
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { ProviderAddedToPool } from './ProviderAddedToPool';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for adding a provider to a pool (#871) - the member names a provider and nothing else: which model a completion runs on is decided by the acting agent's capability tier, translated through the member provider's own tier mapping, so a pool stays a set of interchangeable capacities rather than a grid of provider-and-model pairs.
 */
export interface IAddProviderToPool {
    
    /**
     * The pool to add to.
     */
    pool?: Guid;
    
    /**
     * The provider to add.
     */
    provider?: Guid;
}


/**
 * Command for adding a provider to a pool (#871) - the member names a provider and nothing else: which model a completion runs on is decided by the acting agent's capability tier, translated through the member provider's own tier mapping, so a pool stays a set of interchangeable capacities rather than a grid of provider-and-model pairs.
 */
export class AddProviderToPool extends Command<IAddProviderToPool, ProviderAddedToPool> implements IAddProviderToPool {
    readonly route: string = '/api/a-i/providers/pools/adding-provider';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('pool', Guid, false),
        new PropertyDescriptor('provider', Guid, false),
    ];

    private _pool!: Guid;
    private _provider!: Guid;

    constructor() {
        super(ProviderAddedToPool, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The pool to add to.
     */
    get pool(): Guid {
        return this._pool;
    }

    set pool(value: Guid) {
        this._pool = value;
        this.propertyChanged('pool');
    }
    /**
     * The provider to add.
     */
    get provider(): Guid {
        return this._provider;
    }

    set provider(value: Guid) {
        this._provider = value;
        this.propertyChanged('provider');
    }

    static use(initialValues?: IAddProviderToPool): [AddProviderToPool, SetCommandValues<IAddProviderToPool>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<AddProviderToPool, IAddProviderToPool>(AddProviderToPool, initialValues);
    }
}
