// @generated by Cratis. Source: Cratis.AI.Providers.SettingUsageCapacity.SetAIProviderUsageCapacity. Time: 2026-09-24T20:35:16.9475720Z. Hash: 864F5156431ED611E6781370D982B72BA47F87155ED8264AA514B7C8F7427DF8
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command, CommandValidator } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { AIProviderUsageCapacitySet } from './AIProviderUsageCapacitySet';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for bounding how many tokens a configured AI provider may consume within its usage-reporting window - vendor APIs report consumption, not allowance, so this is what turns a vendor-reported (or, absent a usage credential, locally-burnt) number into "how much room is left", which capacity-aware pool selection ranks members by (issue #1061). Zero means no ceiling, which is what a provider starts with - a provider without one configured simply falls back to the existing least-burnt ranking.
 */
export interface ISetAIProviderUsageCapacity {
    
    /**
     * The provider to bound.
     */
    provider?: Guid;
    
    /**
     * How many tokens it may consume within its usage window - zero for no ceiling.
     */
    usageCapacity?: number;
}

export class SetAIProviderUsageCapacityValidator extends CommandValidator<ISetAIProviderUsageCapacity> {
    constructor() {
        super();
        this.ruleFor(c => c.usageCapacity).greaterThanOrEqual(0).withMessage('A usage capacity cannot be negative - use zero for no ceiling');
    }
}

/**
 * Command for bounding how many tokens a configured AI provider may consume within its usage-reporting window - vendor APIs report consumption, not allowance, so this is what turns a vendor-reported (or, absent a usage credential, locally-burnt) number into "how much room is left", which capacity-aware pool selection ranks members by (issue #1061). Zero means no ceiling, which is what a provider starts with - a provider without one configured simply falls back to the existing least-burnt ranking.
 */
export class SetAIProviderUsageCapacity extends Command<ISetAIProviderUsageCapacity, AIProviderUsageCapacitySet> implements ISetAIProviderUsageCapacity {
    readonly route: string = '/api/a-i/providers/setting-usage-capacity';
    readonly validation: CommandValidator = new SetAIProviderUsageCapacityValidator();
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('provider', Guid, false),
        new PropertyDescriptor('usageCapacity', Number, false),
    ];

    private _provider!: Guid;
    private _usageCapacity!: number;

    constructor() {
        super(AIProviderUsageCapacitySet, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The provider to bound.
     */
    get provider(): Guid {
        return this._provider;
    }

    set provider(value: Guid) {
        this._provider = value;
        this.propertyChanged('provider');
    }
    /**
     * How many tokens it may consume within its usage window - zero for no ceiling.
     */
    get usageCapacity(): number {
        return this._usageCapacity;
    }

    set usageCapacity(value: number) {
        this._usageCapacity = value;
        this.propertyChanged('usageCapacity');
    }

    static use(initialValues?: ISetAIProviderUsageCapacity): [SetAIProviderUsageCapacity, SetCommandValues<ISetAIProviderUsageCapacity>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<SetAIProviderUsageCapacity, ISetAIProviderUsageCapacity>(SetAIProviderUsageCapacity, initialValues);
    }
}
