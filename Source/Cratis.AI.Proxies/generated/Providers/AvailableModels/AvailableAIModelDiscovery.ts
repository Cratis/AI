// @generated by Cratis. Source: Cratis.AI.Providers.AvailableModels.AvailableAIModelDiscovery. Time: 2026-09-24T20:35:17.0313780Z. Hash: 6F53F47A78829634580A3396D2FAB1EEBCF9656D5BD20C13C98F158C49F1436D
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
// eslint-disable-next-line header/header
import { field } from '@cratis/fundamentals';
import { AIModelCapability } from '../AIModelCapability';

/**
 * One row in the discovered catalog, enriched with what Direct knows about the model - what the model pickers in Settings bind to. `[Passive]`, so it is computed on demand from the provider's own events rather than materialized - which is what makes it strongly consistent by key. A refresh appends its event and the next read sees it, with no projection to be behind: the reason the dialog used to need a cache invalidation dance and still showed a stale answer. A sentinel row carries the reason discovery failed, which is what keeps "this provider serves no models" and "nobody could ask" apart on a surface that would otherwise render both as an empty list.
 */
export class AvailableAIModelDiscovery {

    /**
     * The exact model id, or {@link NotSet} for a sentinel row.
     */
    @field(String)
    id!: string;

    /**
     * The friendly model name.
     */
    @field(String)
    displayName!: string;

    /**
     * Known model capabilities.
     */
    @field(Number, true)
    capabilities!: AIModelCapability[];

    /**
     * Whether this row describes a model.
     */
    @field(Boolean)
    hasModel!: boolean;

    /**
     * Why the catalog could not be read, or an empty string when it was.
     */
    @field(String)
    failure!: string;

    /**
     * When the provider was last asked - the default when it never has been.
     */
    @field(Date)
    discoveredAt!: Date;
}
