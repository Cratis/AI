// @generated by Cratis. Source: Cratis.AI.Providers.Copilot.StartCopilotSignIn. Time: 2026-09-24T21:06:58.4767020Z. Hash: 86C9BABF6C16CF7030386AD7F0410FE96A15A727B754F30B75EF22F78C785F00
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { CopilotSignInStarted } from './CopilotSignInStarted';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for signing a Copilot provider in to a GitHub account, using GitHub's device flow. The same reasoning `StartOpenAISubscriptionSignIn` is built on applies here: nobody should have to mint a token somewhere else and paste it into a field labelled "API key" to connect a subscription. Device flow is the flow designed for this - the server has no browser, so a person authorizes from any browser they do have. Pasting a token stays available for deployments with no OAuth app of their own ({@link ReconfigureCopilotProvider}), but it is not the front door.
 */
export interface IStartCopilotSignIn {
    
    /**
     * The provider being signed in.
     */
    provider?: Guid;
}


/**
 * Command for signing a Copilot provider in to a GitHub account, using GitHub's device flow. The same reasoning `StartOpenAISubscriptionSignIn` is built on applies here: nobody should have to mint a token somewhere else and paste it into a field labelled "API key" to connect a subscription. Device flow is the flow designed for this - the server has no browser, so a person authorizes from any browser they do have. Pasting a token stays available for deployments with no OAuth app of their own ({@link ReconfigureCopilotProvider}), but it is not the front door.
 */
export class StartCopilotSignIn extends Command<IStartCopilotSignIn, CopilotSignInStarted> implements IStartCopilotSignIn {
    readonly route: string = '/api/a-i/providers/copilot/start-copilot-sign-in';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('provider', Guid, false),
    ];

    private _provider!: Guid;

    constructor() {
        super(CopilotSignInStarted, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The provider being signed in.
     */
    get provider(): Guid {
        return this._provider;
    }

    set provider(value: Guid) {
        this._provider = value;
        this.propertyChanged('provider');
    }

    static use(initialValues?: IStartCopilotSignIn): [StartCopilotSignIn, SetCommandValues<IStartCopilotSignIn>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<StartCopilotSignIn, IStartCopilotSignIn>(StartCopilotSignIn, initialValues);
    }
}
