// @generated by Cratis. Source: Cratis.AI.Providers.Configuring.AddOpenAICompatibleProvider. Time: 2026-09-15T18:47:57.6447550Z. Hash: 92D668F4E66769F98DF6182262CA31E5073D17A279CBE661769BD574A0ED71ED
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for adding an OpenAI-compatible endpoint (Ollama, LocalAI, a gateway) as an AI provider - configures how to reach it only, not a model. Ported from Studio's own `Settings.AI.Providers.Adding.AddOpenAICompatibleProvider` - see {@link AnthropicModelConfigured}'s remarks for why this vendor family has its own `Configuring` namespace, shaped differently from {@link OpenAICompatibleProviderAdded}.
 */
export interface IAddOpenAICompatibleProvider {
    
    /**
     * The API key, empty when the service needs no credential.
     */
    apiKey?: string;
    
    /**
     * The base URL of the OpenAI-compatible service.
     */
    endpoint?: string;
    
    /**
     * The name the organization knows the provider by.
     */
    name?: string;
}


/**
 * Command for adding an OpenAI-compatible endpoint (Ollama, LocalAI, a gateway) as an AI provider - configures how to reach it only, not a model. Ported from Studio's own `Settings.AI.Providers.Adding.AddOpenAICompatibleProvider` - see {@link AnthropicModelConfigured}'s remarks for why this vendor family has its own `Configuring` namespace, shaped differently from {@link OpenAICompatibleProviderAdded}.
 */
export class AddOpenAICompatibleProvider extends Command<IAddOpenAICompatibleProvider, Guid> implements IAddOpenAICompatibleProvider {
    readonly route: string = '/api/a-i/providers/configuring/add-open-a-i-compatible-provider';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('apiKey', String, false),
        new PropertyDescriptor('endpoint', String, false),
        new PropertyDescriptor('name', String, false),
    ];

    private _apiKey!: string;
    private _endpoint!: string;
    private _name!: string;

    constructor() {
        super(Guid, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The API key, empty when the service needs no credential.
     */
    get apiKey(): string {
        return this._apiKey;
    }

    set apiKey(value: string) {
        this._apiKey = value;
        this.propertyChanged('apiKey');
    }
    /**
     * The base URL of the OpenAI-compatible service.
     */
    get endpoint(): string {
        return this._endpoint;
    }

    set endpoint(value: string) {
        this._endpoint = value;
        this.propertyChanged('endpoint');
    }
    /**
     * The name the organization knows the provider by.
     */
    get name(): string {
        return this._name;
    }

    set name(value: string) {
        this._name = value;
        this.propertyChanged('name');
    }

    static use(initialValues?: IAddOpenAICompatibleProvider): [AddOpenAICompatibleProvider, SetCommandValues<IAddOpenAICompatibleProvider>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<AddOpenAICompatibleProvider, IAddOpenAICompatibleProvider>(AddOpenAICompatibleProvider, initialValues);
    }
}
