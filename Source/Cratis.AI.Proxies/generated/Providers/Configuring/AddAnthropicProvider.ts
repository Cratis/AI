// @generated by Cratis. Source: Cratis.AI.Providers.Configuring.AddAnthropicProvider. Time: 2026-09-23T18:02:22.4490540Z. Hash: C53A8F651C57A49A1EC6B4C106116D9006F734752418F0DA308D354D3E5D0E02
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for adding Anthropic as an AI provider - configures the credential only, not a model: which model to run is chosen per agent. Ported from Studio's own `Settings.AI.Providers.Adding.AddAnthropicProvider` (plan Section 5.2 step 4), which shapes this differently from Direct's donor - see {@link AnthropicModelConfigured}'s own remarks for why this whole vendor family lives in its own `Configuring` namespace rather than reusing {@link AnthropicProviderAdded}.
 */
export interface IAddAnthropicProvider {
    
    /**
     * The Anthropic API key.
     */
    apiKey?: string;
    
    /**
     * The name the organization knows the provider by.
     */
    name?: string;
}


/**
 * Command for adding Anthropic as an AI provider - configures the credential only, not a model: which model to run is chosen per agent. Ported from Studio's own `Settings.AI.Providers.Adding.AddAnthropicProvider` (plan Section 5.2 step 4), which shapes this differently from Direct's donor - see {@link AnthropicModelConfigured}'s own remarks for why this whole vendor family lives in its own `Configuring` namespace rather than reusing {@link AnthropicProviderAdded}.
 */
export class AddAnthropicProvider extends Command<IAddAnthropicProvider, Guid> implements IAddAnthropicProvider {
    readonly route: string = '/api/a-i/providers/configuring/add-anthropic-provider';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('apiKey', String, false),
        new PropertyDescriptor('name', String, false),
    ];

    private _apiKey!: string;
    private _name!: string;

    constructor() {
        super(Guid, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The Anthropic API key.
     */
    get apiKey(): string {
        return this._apiKey;
    }

    set apiKey(value: string) {
        this._apiKey = value;
        this.propertyChanged('apiKey');
    }
    /**
     * The name the organization knows the provider by.
     */
    get name(): string {
        return this._name;
    }

    set name(value: string) {
        this._name = value;
        this.propertyChanged('name');
    }

    static use(initialValues?: IAddAnthropicProvider): [AddAnthropicProvider, SetCommandValues<IAddAnthropicProvider>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<AddAnthropicProvider, IAddAnthropicProvider>(AddAnthropicProvider, initialValues);
    }
}
