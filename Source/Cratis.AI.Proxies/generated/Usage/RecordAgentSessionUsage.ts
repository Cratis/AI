// @generated by Cratis. Source: Cratis.AI.Usage.RecordAgentSessionUsage. Time: 2026-09-24T20:35:16.9475720Z. Hash: 00C528E8CDF98B4B0BF929FA9126FE735831CC039DF99C4D50382C693DFFA5F6
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
/* eslint-disable @typescript-eslint/no-empty-interface */
// eslint-disable-next-line header/header
import { Command, CommandValidator } from '@cratis/arc/commands';
import { useCommand, SetCommandValues, ClearCommandValues } from '@cratis/arc.react/commands';
import { PropertyDescriptor } from '@cratis/arc/reflection';
import { Harness } from '../Harnesses/Harness';
import { Guid } from '@cratis/fundamentals';

/**
 * Command for recording one agent session's usage - accepts the full harness callback payload shape (`status, detail, inputTokens, outputTokens, costUsd, durationMs, cpuSeconds, memoryBytes`, plus the resolution context a direct completion already has to hand) so both harness-run worker sessions and in-process completions record through the one command (plan Section 5.3c).
 */
export interface IRecordAgentSessionUsage {
    
    /**
     * The {@link AgentId} that did the work.
     */
    agent?: string;
    
    /**
     * Prompt tokens served from the provider's own cache.
     */
    cachedTokens?: number;
    
    /**
     * The reported cost, in USD.
     */
    costUsd?: number;
    
    /**
     * The CPU time a harness measured for the session.
     */
    cpuSeconds?: number;
    
    /**
     * How long the session took.
     */
    duration?: number;
    
    /**
     * The {@link Harness} the session ran under, when it was a harness-run job.
     */
    harness?: Harness;
    
    /**
     * Tokens the prompt consumed.
     */
    inputTokens?: number;
    
    /**
     * The peak memory a harness measured for the session.
     */
    memoryBytes?: number;
    
    /**
     * The model that served it.
     */
    model?: string;
    
    /**
     * Tokens the completion produced.
     */
    outputTokens?: number;
    
    /**
     * The {@link AIProviderId} that served it, when resolved through a configured provider.
     */
    provider?: Guid;
    
    /**
     * What the call was for.
     */
    purpose?: string;
    
    /**
     * The {@link AgentSessionId} the usage belongs to.
     */
    session?: string;
}

export class RecordAgentSessionUsageValidator extends CommandValidator<IRecordAgentSessionUsage> {
    constructor() {
        super();
        this.ruleFor(c => c.cachedTokens).greaterThanOrEqual(0).withMessage('Cached tokens cannot be negative');
        this.ruleFor(c => c.costUsd).greaterThanOrEqual(0).withMessage('A cost cannot be negative');
        this.ruleFor(c => c.costUsd).lessThanOrEqual(100000).withMessage('A cost cannot exceed $100,000');
        this.ruleFor(c => c.cpuSeconds).greaterThanOrEqual(0).withMessage('A CPU time cannot be negative');
        this.ruleFor(c => c.cpuSeconds).lessThanOrEqual(1000000).withMessage('A CPU time cannot exceed 1,000,000 seconds');
        this.ruleFor(c => c.duration).greaterThanOrEqual(0).withMessage('A duration cannot be negative');
        this.ruleFor(c => c.inputTokens).greaterThanOrEqual(0).withMessage('Input tokens cannot be negative');
        this.ruleFor(c => c.memoryBytes).greaterThanOrEqual(0).withMessage('A memory size cannot be negative');
        this.ruleFor(c => c.memoryBytes).lessThanOrEqual(1099511627776).withMessage('A memory size cannot exceed 1 TiB');
        this.ruleFor(c => c.outputTokens).greaterThanOrEqual(0).withMessage('Output tokens cannot be negative');
    }
}

/**
 * Command for recording one agent session's usage - accepts the full harness callback payload shape (`status, detail, inputTokens, outputTokens, costUsd, durationMs, cpuSeconds, memoryBytes`, plus the resolution context a direct completion already has to hand) so both harness-run worker sessions and in-process completions record through the one command (plan Section 5.3c).
 */
export class RecordAgentSessionUsage extends Command<IRecordAgentSessionUsage, string> implements IRecordAgentSessionUsage {
    readonly route: string = '/api/a-i/usage';
    readonly validation: CommandValidator = new RecordAgentSessionUsageValidator();
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly propertyDescriptors: PropertyDescriptor[] = [
        new PropertyDescriptor('agent', String, false),
        new PropertyDescriptor('cachedTokens', Number, true),
        new PropertyDescriptor('costUsd', Number, true),
        new PropertyDescriptor('cpuSeconds', Number, true),
        new PropertyDescriptor('duration', Number, true),
        new PropertyDescriptor('harness', Number, true),
        new PropertyDescriptor('inputTokens', Number, true),
        new PropertyDescriptor('memoryBytes', Number, true),
        new PropertyDescriptor('model', String, false),
        new PropertyDescriptor('outputTokens', Number, true),
        new PropertyDescriptor('provider', Guid, true),
        new PropertyDescriptor('purpose', String, false),
        new PropertyDescriptor('session', String, false),
    ];

    private _agent!: string;
    private _cachedTokens?: number;
    private _costUsd?: number;
    private _cpuSeconds?: number;
    private _duration?: number;
    private _harness?: Harness;
    private _inputTokens?: number;
    private _memoryBytes?: number;
    private _model!: string;
    private _outputTokens?: number;
    private _provider?: Guid;
    private _purpose!: string;
    private _session!: string;

    constructor() {
        super(String, false);
    }

    get requestParameters(): string[] {
        return [
        ];
    }

    /**
     * The {@link AgentId} that did the work.
     */
    get agent(): string {
        return this._agent;
    }

    set agent(value: string) {
        this._agent = value;
        this.propertyChanged('agent');
    }
    /**
     * Prompt tokens served from the provider's own cache.
     */
    get cachedTokens(): number | undefined {
        return this._cachedTokens;
    }

    set cachedTokens(value: number | undefined) {
        this._cachedTokens = value;
        this.propertyChanged('cachedTokens');
    }
    /**
     * The reported cost, in USD.
     */
    get costUsd(): number | undefined {
        return this._costUsd;
    }

    set costUsd(value: number | undefined) {
        this._costUsd = value;
        this.propertyChanged('costUsd');
    }
    /**
     * The CPU time a harness measured for the session.
     */
    get cpuSeconds(): number | undefined {
        return this._cpuSeconds;
    }

    set cpuSeconds(value: number | undefined) {
        this._cpuSeconds = value;
        this.propertyChanged('cpuSeconds');
    }
    /**
     * How long the session took.
     */
    get duration(): number | undefined {
        return this._duration;
    }

    set duration(value: number | undefined) {
        this._duration = value;
        this.propertyChanged('duration');
    }
    /**
     * The {@link Harness} the session ran under, when it was a harness-run job.
     */
    get harness(): Harness | undefined {
        return this._harness;
    }

    set harness(value: Harness | undefined) {
        this._harness = value;
        this.propertyChanged('harness');
    }
    /**
     * Tokens the prompt consumed.
     */
    get inputTokens(): number | undefined {
        return this._inputTokens;
    }

    set inputTokens(value: number | undefined) {
        this._inputTokens = value;
        this.propertyChanged('inputTokens');
    }
    /**
     * The peak memory a harness measured for the session.
     */
    get memoryBytes(): number | undefined {
        return this._memoryBytes;
    }

    set memoryBytes(value: number | undefined) {
        this._memoryBytes = value;
        this.propertyChanged('memoryBytes');
    }
    /**
     * The model that served it.
     */
    get model(): string {
        return this._model;
    }

    set model(value: string) {
        this._model = value;
        this.propertyChanged('model');
    }
    /**
     * Tokens the completion produced.
     */
    get outputTokens(): number | undefined {
        return this._outputTokens;
    }

    set outputTokens(value: number | undefined) {
        this._outputTokens = value;
        this.propertyChanged('outputTokens');
    }
    /**
     * The {@link AIProviderId} that served it, when resolved through a configured provider.
     */
    get provider(): Guid | undefined {
        return this._provider;
    }

    set provider(value: Guid | undefined) {
        this._provider = value;
        this.propertyChanged('provider');
    }
    /**
     * What the call was for.
     */
    get purpose(): string {
        return this._purpose;
    }

    set purpose(value: string) {
        this._purpose = value;
        this.propertyChanged('purpose');
    }
    /**
     * The {@link AgentSessionId} the usage belongs to.
     */
    get session(): string {
        return this._session;
    }

    set session(value: string) {
        this._session = value;
        this.propertyChanged('session');
    }

    static use(initialValues?: IRecordAgentSessionUsage): [RecordAgentSessionUsage, SetCommandValues<IRecordAgentSessionUsage>, ClearCommandValues] {
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        return useCommand<RecordAgentSessionUsage, IRecordAgentSessionUsage>(RecordAgentSessionUsage, initialValues);
    }
}
