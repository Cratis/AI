# Cratis Pi integration

This package is the native Pi integration surface for Cratis skills. It does not
choose profiles or languages. `cratis ai install` and `cratis ai update` resolve
`.cratis/ai.json` and write only the resulting skill paths into `.pi/settings.json`.
