name: Claude Assistant (inactive template)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inactive:
    runs-on: ubuntu-latest
    steps:
      - name: Explain activation boundary
        run: |
          echo "This historical template is intentionally inactive."
          echo "Issue, pull-request, repository, token, and model effects require a separately reviewed workflow and exact repository operation policy."
