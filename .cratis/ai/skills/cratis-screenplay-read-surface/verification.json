{
  "skill": "cratis-screenplay-read-surface",
  "input": "Declare a Screenplay query and the screen that binds to it.",
  "fixtures": [],
  "assertions": [
    { "kind": "skill-contains", "value": "The tenant is the default and stays unstated" },
    { "kind": "skill-contains", "value": "belongs on a `from` parameter, never on a `filter`" },
    { "kind": "skill-contains", "value": "warnings, not errors" }
  ]
}
