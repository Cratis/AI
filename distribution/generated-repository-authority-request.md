# Generated distribution repository authority state

## Created repository

Public repository [`Cratis/AI.Distribution`](https://github.com/Cratis/AI.Distribution)
exists as the empty bot-owned projection boundary for approved `Cratis/AI`
source. It has no branch, commit, or manually authored file.

Repository creation is registered with Strategy through
[`Cratis/Strategy#126`](https://github.com/Cratis/Strategy/issues/126), so
Strategy can apply its own rules and skills to repository metadata, ownership,
portfolio placement, and AI setup.

## Configured authority

- A repository-scoped read/write deploy key named `Cratis AI distribution bot`
  is attached only to `Cratis/AI.Distribution`.
- Its private key is stored only as `AI_DISTRIBUTION_DEPLOY_KEY` in `Cratis/AI`
  Actions secrets.
- `distribution-canary` and `npm-stage` environments exist in `Cratis/AI`, are
  restricted to `main`, and require the named maintainer reviewer.
- Secret scanning and push protection are enabled on the public distribution
  repository.
- Issues, projects, and wiki are disabled; merge commits are the only enabled PR
  merge strategy.

## Initialization sequence

The canonical `Cratis/AI` workflow may use the deploy key only after environment
approval to push the deterministic fixture-only root commit generated by
`tooling/bootstrap-generated-distribution-repository.mjs`. After `main` exists,
protect it against direct pushes and require reviewed generated updates.

The deploy key intentionally cannot create pull requests, releases, tags, or
package publications. A repository-scoped GitHub App or equivalent bot identity
is still required before generated update PRs and release operations can begin.

## Gates that remain closed

- Zero public targets or product-source contracts are approved.
- The planned public artifact remains materialization and runtime disabled.
- `@cratis/ai` ownership and npm trusted publishing are not configured.
- No production consumer canary or rollback has run.
- Publication, promotion, fleet activation, legacy retirement, and freeze
  lifting remain disabled.
