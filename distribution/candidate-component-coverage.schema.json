{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Cratis/AI/distribution/candidate-component-coverage.schema.json",
  "title": "Cratis candidate component coverage",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "schemaPath",
    "schemaSha256",
    "state",
    "inputDigests",
    "componentCount",
    "byKind",
    "byDisposition",
    "records",
    "approvalGranted",
    "installationSupported",
    "publicationEligible",
    "runtimeEligible",
    "supportGranted",
    "promotionEligible"
  ],
  "properties": {
    "schemaVersion": { "const": "1.0.0" },
    "schemaPath": { "const": "distribution/candidate-component-coverage.schema.json" },
    "schemaSha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "state": { "const": "CANDIDATE_COMPONENT_COVERAGE_REVIEW_ONLY" },
    "inputDigests": {
      "type": "object",
      "additionalProperties": false,
      "required": ["components", "projections", "artifacts"],
      "properties": {
        "components": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "projections": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "artifacts": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "$comment": "No cardinality is pinned here. The reviewed sizes live once in distribution/candidate-component-coverage.seals.json and are enforced by tooling/component-inventory-counts.mjs, which throws before this document can be produced. Restating them here made every content migration edit two files that had to agree, and they stopped agreeing (records.minItems sat at 178 while the builder required exactly 182).",
    "componentCount": { "type": "integer", "minimum": 1 },
    "byKind": {
      "type": "object",
      "additionalProperties": false,
      "required": ["agent", "command", "executable-host-extension", "hook", "instruction", "prompt", "rule", "skill"],
      "properties": {
        "agent": { "type": "integer", "minimum": 0 },
        "command": { "type": "integer", "minimum": 0 },
        "executable-host-extension": { "type": "integer", "minimum": 0 },
        "hook": { "type": "integer", "minimum": 0 },
        "instruction": { "type": "integer", "minimum": 0 },
        "prompt": { "type": "integer", "minimum": 0 },
        "rule": { "type": "integer", "minimum": 0 },
        "skill": { "type": "integer", "minimum": 0 }
      }
    },
    "byDisposition": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "executable-blocked",
        "native-static-review-projected",
        "native-static-unprojected",
        "repository-host-adapter-only",
        "skill-blocked-candidate",
        "skill-legacy-repository-only",
        "skill-packaged-candidate"
      ],
      "properties": {
        "executable-blocked": { "type": "integer", "minimum": 0 },
        "native-static-review-projected": { "type": "integer", "minimum": 0 },
        "native-static-unprojected": { "type": "integer", "minimum": 0 },
        "repository-host-adapter-only": { "type": "integer", "minimum": 0 },
        "skill-blocked-candidate": { "type": "integer", "minimum": 0 },
        "skill-legacy-repository-only": { "type": "integer", "minimum": 0 },
        "skill-packaged-candidate": { "type": "integer", "minimum": 0 }
      }
    },
    "records": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "componentId",
          "kind",
          "audience",
          "lifecycle",
          "releaseBoundary",
          "disposition",
          "reason",
          "existingProjectionCount",
          "generatedStaticProjectionCount",
          "approvalState",
          "runtimeEligible",
          "publicationEligible",
          "supportGranted"
        ],
        "properties": {
          "componentId": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
          "kind": { "enum": ["agent", "command", "executable-host-extension", "hook", "instruction", "prompt", "rule", "skill"] },
          "audience": { "enum": ["public", "cratis-engineering"] },
          "lifecycle": { "enum": ["active", "legacy-retained"] },
          "releaseBoundary": { "enum": ["public-passive", "engineering-passive", "repository-only", "future-executable-package"] },
          "disposition": { "enum": ["skill-packaged-candidate", "skill-blocked-candidate", "skill-legacy-repository-only", "native-static-review-projected", "native-static-unprojected", "repository-host-adapter-only", "executable-blocked"] },
          "reason": { "enum": ["included-in-passive-review-candidate", "private-or-local-content", "mcp-guidance-materialization-blocked", "incomplete-resource-and-license-closure", "legacy-retained-repository-only", "generated-static-repository-fixture", "no-generated-static-contract", "repository-native-kind-no-package-contract", "executable-component-blocked"] },
          "existingProjectionCount": { "type": "integer", "minimum": 0 },
          "generatedStaticProjectionCount": { "type": "integer", "minimum": 0 },
          "approvalState": { "enum": ["modeled", "blocked"] },
          "runtimeEligible": { "const": false },
          "publicationEligible": { "const": false },
          "supportGranted": { "const": false }
        }
      }
    },
    "approvalGranted": { "const": false },
    "installationSupported": { "const": false },
    "publicationEligible": { "const": false },
    "runtimeEligible": { "const": false },
    "supportGranted": { "const": false },
    "promotionEligible": { "const": false }
  }
}
