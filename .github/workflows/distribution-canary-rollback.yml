# Copyright (c) Cratis. All rights reserved.
# Licensed under the MIT license. See LICENSE file in the project root for full license information.

name: Verify Distribution Canary and Rollback

on:
  workflow_dispatch:
    inputs:
      operation:
        description: Fixture-only operation
        required: true
        default: simulate-canary-rollback
        type: choice
        options:
          - verify-candidate
          - simulate-canary-rollback
          - simulate-generated-repository
          - initialize-remote-fixture

permissions:
  contents: read

concurrency:
  group: distribution-fixture-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify-fixture:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out the canonical authoring repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Use Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Verify candidate staging
        if: inputs.operation == 'verify-candidate'
        run: |
          set -euo pipefail
          node tooling/stage-distribution-candidate.mjs \
            --artifact sanitized-public-materializer-fixture \
            --output "$RUNNER_TEMP/distribution-candidate" \
            --record "$RUNNER_TEMP/distribution-candidate.json"
          node tooling/generate-distribution-fixture.mjs \
            "$RUNNER_TEMP/independent-distribution-candidate"
          diff -u \
            "$RUNNER_TEMP/distribution-candidate/distribution-manifest.json" \
            "$RUNNER_TEMP/independent-distribution-candidate/distribution-manifest.json"

      - name: Simulate canary promotion and rollback
        if: inputs.operation == 'simulate-canary-rollback'
        run: |
          set -euo pipefail
          node tooling/simulate-distribution-rollout.mjs \
            "$RUNNER_TEMP/distribution-rollout"

      - name: Simulate the bot-owned generated repository
        if: inputs.operation == 'simulate-generated-repository'
        run: |
          set -euo pipefail
          node tooling/bootstrap-generated-distribution-repository.mjs \
            --output "$RUNNER_TEMP/generated-distribution-repository" \
            --record "$RUNNER_TEMP/generated-distribution-repository.json"
          test "$(git -C "$RUNNER_TEMP/generated-distribution-repository" rev-list --count HEAD)" = "1"
          test -z "$(git -C "$RUNNER_TEMP/generated-distribution-repository" status --porcelain)"

      - name: Run distribution specifications
        run: |
          node --test tooling/specs/distribution-fixture.spec.mjs
          node --test tooling/specs/distribution-rollout.spec.mjs
          node --test tooling/specs/generated-distribution-repository.spec.mjs

      - name: Confirm publication remains absent
        run: |
          set -euo pipefail
          test "$(node -p "require('./distribution/rollout-policy.json').promotion.publicationEnabled")" = "false"
          test "$(node -p "require('./distribution/rollout-policy.json').legacyRetirement.enabled")" = "false"

  initialize-remote-fixture:
    if: inputs.operation == 'initialize-remote-fixture'
    needs: verify-fixture
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: distribution-canary
    steps:
      - name: Check out the canonical authoring repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Use Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Build the generated fixture repository
        run: |
          set -euo pipefail
          node tooling/bootstrap-generated-distribution-repository.mjs \
            --output "$RUNNER_TEMP/generated-distribution-repository" \
            --record "$RUNNER_TEMP/generated-distribution-repository.json"

      - name: Configure the repository-scoped deploy key
        env:
          DISTRIBUTION_DEPLOY_KEY: ${{ secrets.AI_DISTRIBUTION_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          install -m 700 -d "$HOME/.ssh"
          printf '%s\n' "$DISTRIBUTION_DEPLOY_KEY" > "$HOME/.ssh/id_ed25519"
          chmod 600 "$HOME/.ssh/id_ed25519"
          ssh-keyscan -t ed25519 github.com > "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/known_hosts"

      - name: Initialize the empty remote from generated bytes
        env:
          GIT_SSH_COMMAND: ssh -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes -o BatchMode=yes
        run: |
          set -euo pipefail
          git -C "$RUNNER_TEMP/generated-distribution-repository" remote add origin \
            git@github.com:Cratis/AI.Distribution.git
          git -C "$RUNNER_TEMP/generated-distribution-repository" push origin main
